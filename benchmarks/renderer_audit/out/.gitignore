# The audit's working images are reproducible from the scenes (see REPORT.md
# section 7) and there are megabytes of them, so only the contact sheets the
# report embeds are tracked.
*
!.gitignore
!*.compare.jpg
# Not a contact sheet: two committed baselines side by side, which no scene in
# this tree reproduces (see REPORT.md section 2.2).
!colour_decode_before_after.jpg
